name: ecommerce_app
description: A new Flutter project.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.2

  # Firebase (LOCKED to compatible versions)
  firebase_core: ^2.32.0
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.17.5

  # UI
  shimmer: ^3.0.0

  # Payments (DOWNGRADED to avoid conflicts)
  flutterwave_standard: ^1.0.7